{
  "name": "klipit",
  "version": "1.0.0",
  "private": true,
  "description": "KLIPIT — paste a stream link, get clips of the best moments, download and post anywhere.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "worker": "node jobs/worker.js",
    "dev": "node server.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.4",
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "stripe": "^16.9.0"
  }
}
