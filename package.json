{
  "name": "klipit",
  "version": "1.0.0",
  "description": "Klipit by HSW365 — paste a GTA6 stream link, get subscription-gated clips. Claude API + Stripe only.",
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node server/server.js",
    "stripe:setup": "node scripts/stripe-setup.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.1",
    "@supabase/supabase-js": "^2.45.4",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "stripe": "^17.2.0"
  }
}
